# DrawAim 项目开发文档

> 文档版本：0.2  
> 文档日期：2026-08-03  
> 产品名称：DrawAim（暂定）  
> 目标版本：首个可持续训练、可正式构建和发布的 Windows 桌面版本

## 1. 文档目的

本文档是 DrawAim 的产品、交互、算法、工程和验收依据。实现过程中不得用静态界面、随机百分比、假按钮或占位流程代替真实功能。

本文使用以下优先级词：

- 必须：首个正式版本不可缺少，缺少即不算完成。
- 应当：除非有明确技术证据，否则应实现。
- 可以：不影响核心闭环时再实现。

发生冲突时，按以下顺序裁决：

1. 玩家使用感受、绘画手感和反馈可信度；
2. 低输入延迟、稳定帧率和数位笔兼容性；
3. 评分公平、生成内容有效；
4. 界面清晰、美观、易理解；
5. 开发速度。

## 2. 产品目标

DrawAim 是一款面向绘画基本功训练的桌面软件，核心目标是持续训练：

- 单条线的控制能力；
- 对多条线组合的观察、位置判断和复制能力；
- 对颜色明度、饱和度、感知彩度和色相的判断能力。

软件必须形成连续、低打断的训练闭环，而不是普通绘图板、自由绘画软件或算法演示。

首个正式版本包含：

1. 模式一：单条线跟随训练；
2. 模式二：观察并复制线条组合；
3. 模式三：颜色匹配训练；
4. 三种模式共享的高质量鼠标、数位笔、压力和笔迹稳定输入系统；
5. 设置、会话统计、历史最佳、主题、首次引导和本地保存；
6. 自动化测试、正式构建、Windows 可运行发布产物和 README。

首版明确不做：

- 账号、登录、云同步、联网排行榜；
- 广告、遥测和数据上传；
- 通用图片编辑、图层系统和复杂笔刷商店；
- 根据目标答案吸附、磁吸、自动描线或自动补全；
- HDR、专业印刷色彩管理和显示器校色工具。

模式三首版以 SDR sRGB、D65 白点为显示和计算基准。未经实体设备验证，不得宣称达到专业校色显示器的绝对色彩精度。

## 3. 平台、运行环境与发布目标

- 平台：Windows 10/11 桌面端；
- 架构：首发 win-x64；
- 界面：简体中文；
- 运行：完全离线；
- 输入：鼠标左键、数位笔和压感笔；
- 显示：高 DPI、每显示器 DPI、1920×1080 与 2560×1440；
- 发布：优先提供无需额外安装运行时的自包含便携版本；
- 数据：只保存在用户本机。

Windows 10 的最低版本、具体已验证版本和未验证版本必须在 README 与构建报告中如实列出，不能用单台 Windows 11 机器的结果证明所有 Windows 10/11 环境均已验证。

## 4. 技术方案与架构

### 4.1 默认技术基线

空工作区默认采用：

- C#；
- .NET 10；
- WPF；
- 自定义高性能绘画控件；
- 核心算法与 WPF UI 分离；
- x64 自包含发布。

选择该方案的原因：

- Windows 桌面发布成熟；
- 可直接处理鼠标、Stylus 和压力数据；
- 适合使用 DrawingVisual、DrawingContext 或自定义渲染元素增量绘制；
- 数学、生成、评分和颜色算法可在无 UI 环境中测试；
- 本地设置、历史和发布链路简单。

开始大规模功能开发前，必须先完成共享输入与绘制垂直切片，并验证合并输入点、数位笔模拟鼠标去重、高 DPI 坐标和持续绘制性能。如果量化结果无法达到本文性能预算，必须记录技术决策并在模式二、三开发前更换输入或渲染实现，不能在全部功能完成后才发现基础绘画手感不合格。

### 4.2 建议工程边界

解决方案至少划分为：

- DrawAim.App：WPF 窗口、页面、控件、输入桥接和渲染；
- DrawAim.Core：几何、随机生成、笔迹处理、稳定器、评分、状态机和颜色算法；
- DrawAim.Infrastructure：设置、历史、日志和本地文件；
- DrawAim.Tests：核心单元测试、基础设施测试和集成测试。

DrawAim.Core 不得引用 WPF 类型。核心坐标使用双精度 Point2 或等价结构。

### 4.3 共用处理管线

    原始鼠标或数位笔历史采样
    → 设备识别与模拟事件去重
    → 画布局部 DIP 坐标
    → 非法点、重复点和时间戳清理
    → 可选 StrokeStabilizerV1
    → 不可变 LogicalStroke 中心轨迹
    → 增量显示
    → 弧长重采样
    → 模式评分

显示和评分必须使用同一条 LogicalStroke。压力、抗锯齿、笔刷纹理和显示笔宽不得改变中心线几何评分。

## 5. 共用输入系统

### 5.1 输入规则

- 鼠标只接受左键；
- 数位笔只接受正常笔尖接触；
- 右键、触摸、掌压、悬停、橡皮端和无关侧键不作为绘画输入；
- 鼠标压力固定为 1；
- 数位笔压力规范化并限制在 0～1；
- 压力缺失或异常时使用安全默认值，但不能丢失合法轨迹；
- 压力只影响显示笔宽，不影响几何轨迹和几何评分。

必须优先读取事件中可获得的全部历史或合并输入点。标准 API 无法提供每个历史包真实时间戳时，可以使用事件时间并进行明确、确定的插值，不得伪造无法保证的硬件时间精度。

### 5.2 设备去重

数位笔事件可能继续产生模拟鼠标事件。输入层必须：

- 优先根据事件关联的 Stylus 或 Pointer 设备识别提升鼠标事件；
- 同一时刻只允许一个活动输入源；
- 时间窗口只能作为辅助兜底，不能作为唯一判断；
- 输入源结束后再安全释放设备锁；
- 通过测试保证一次落笔不会生成两条笔迹。

### 5.3 捕获和系统取消

正式落笔后必须捕获当前输入设备。以下情况属于 SystemCancelled：

- Pointer 或 Stylus Capture 丢失；
- 窗口失去激活；
- Alt+Tab 或最小化；
- 绘画中打开会破坏画布的设置；
- 画布尺寸或 DPI 在绘画中改变；
- 输入设备异常断开；
- 应用关闭。

SystemCancelled 不等同于玩家主动提交，不计分、不计入已完成题数。恢复后回到安全的 Ready 或 Paused 状态。

模式一从画布内开始后，可以继续记录移出画布的捕获坐标；越界部分参与偏离和多余轨迹惩罚，但显示时裁剪到画布区域。玩家在画布外松开仍应正确结束本题。

### 5.4 数据安全

输入清理必须处理：

- NaN、Infinity 和异常大坐标；
- 完全重复点和近零距离点；
- 单点和双点笔迹；
- 重复、倒退和溢出的时间戳；
- 压力越界；
- 极高采样率和极长笔迹。

每点处理应为常数级，不得在输入热路径中反复拟合整条曲线或大量创建 UI 对象。达到点数软上限后应进行保形在线压缩，不能无限增长内存，也不能突然删除整笔答案。

## 6. 笔迹防抖／修线 0～100

### 6.1 产品定义

设置名称为“笔迹稳定”，使用整数滑块 0～100，默认值为 0，并显示当前数值和文字等级。

- 0：完全关闭，不进行几何防抖；
- 1～24：轻微，主要减少细小高频抖动；
- 25～49：中等，能够看出笔迹更平滑；
- 50～74：明显，产生清楚的稳定和跟随感；
- 75～99：强，明显抑制急促转向和细碎抖动；
- 100：极强，稳定感、跟随感和转弯圆化必须一眼可辨。

100 仍然不能：

- 读取目标答案；
- 向目标线吸附；
- 自动拉直；
- 自动补全；
- 自动修正到标准线；
- 把明显错误的输入变成正确答案。

无论数值多高，输出都只能由玩家真实输入决定。

界面提供 0、25、50、75、100 快捷刻度。模式一和模式二分别记忆上次使用值；模式三可以独立记忆试色画布的稳定值。

### 6.2 生效时机

- 一笔开始时锁定稳定值；
- 落笔过程中调整滑块不改变当前笔；
- 模式一修改值后从下一题生效；
- 模式二修改值从下一笔生效，已有最终几何不重算；历史记录本题是否用过稳定辅助以及实际使用过的最大稳定值；
- 模式三修改值从下一笔生效，已有试色笔迹无需重算，因为试色画布不参与颜色评分。

### 6.3 显示与评分一致性

- 稳定值为 0 时，LogicalStroke 等于清理后的原始中心轨迹；
- 稳定值大于 0 时，显示和评分都使用稳定后的 LogicalStroke；
- 不得显示稳定后的线，却偷偷使用原始线评分；
- 原始点只可用于诊断和兼容性日志；
- 模式一、二的成绩必须保存稳定值和稳定算法版本；
- 稳定值 0 记为无辅助成绩；
- 稳定值 1～100 记为稳定辅助成绩；
- 无辅助最佳与辅助最佳不得混排；
- 模式三的稳定值不影响颜色成绩。

### 6.4 StrokeStabilizerV1

首版使用与目标答案无关、只依赖历史输入的因果自适应低通算法。不等待未来输入包，不使用离线重拟合替换玩家抬笔前看到的轨迹。

令稳定强度为 N：

    s = clamp(N / 100, 0, 1)

当 N 等于 0 时直接旁路。

当 N 大于 0 时使用：

    tauBase = 0.080 × s² 秒

    speed = distance(raw[i], raw[i-1]) / dt

    tauEffective =
        tauBase /
        (1 + 0.75 × min(speed / 1000 DIP/s, 2))

    alpha = 1 - exp(-dt / tauEffective)

    stable[i] =
        stable[i-1] + alpha × (raw[i] - stable[i-1])

为了防止快速移动时无限落后，增加空间跟随上限：

    maxLag = 1 + 31 × s^1.5 DIP

若原始位置与稳定位置距离超过 maxLag，将稳定位置沿二者连线前移，使距离回到上限以内。

补充要求：

- 每笔第一个稳定点等于第一个有效原始点；
- dt 小于等于 0、时间戳倒退和非法数必须安全处理；
- 抬笔时处理最后一个输入样本，但不得吸附目标或重新拟合整笔；
- 提交时的轨迹与抬笔前最后看到的轨迹一致；
- 新题、清空和系统取消必须重置滤波器状态；
- 每点处理为 O(1)，输入热路径避免分配；
- 参数校准后必须固定并标记 StrokeStabilizerV1；
- 未来改变算法或参数映射时升级版本，历史成绩不得静默混用。

稳定值较高时，可以显示低干扰的“实际落墨点”标记，帮助玩家区分硬件光标位置与稳定后的落墨位置。

### 6.5 稳定器验收

- 0 必须真正旁路，不增加固定缓冲；
- 0、25、50、75、100 对固定抖动轨迹的残余高频抖动总体单调下降；
- 100 在基准抖动轨迹上的高频 RMS 至少比 0 低 60%；
- 60、120、240、480 Hz 的同一参数化轨迹经统一重采样后，稳定轨迹平均距离不超过 1 DIP；
- 不同采样率导致的模式一总分差不超过 1 分；
- 原始点和稳定点距离不得超过当前 maxLag；
- 相同输入、时间戳、强度和版本产生相同输出；
- 相同输入面对不同目标线时，稳定输出必须完全相同；
- 只改变压力不得改变中心轨迹；
- Release 下每批 64 点的稳定处理 P95 目标小于 0.5 ms；
- 0、25、50、75、100 必须通过鼠标和实体数位笔主观手感检查。

## 7. 模式一：单条线跟随训练

### 7.1 答案与提交规则

每题只生成一条目标线，每题只允许一笔答案。

- 任意画布内鼠标左键按下或笔尖接触即开始；
- 起点和推荐方向只是视觉提示，不是落笔门槛；
- 玩家可以正向或反向描画；
- 第一次鼠标左键松开或笔尖抬起并结束接触时，立即冻结答案并自动提交；
- 极短笔迹、只点一下或未完成都属于有效答案并获得相应低分；
- 一旦提交不能补第二笔；
- 模式一不提供本题撤销、重做或清空；
- 系统取消不提交、不计分；
- 提交后显示约 500 ms 反馈并自动进入下一题；
- 新题必须等待上一笔完全松开后才能接受输入。

绘画中提供低成本、低干扰的实时反馈，例如当前目标覆盖进度和明显偏离提示；完整的最终评分只在抬笔后计算，实时反馈不得阻塞输入或改变答案。

推荐方向不参与评分。相同几何轨迹正向和反向绘制应得到近似相同的分数。

### 7.2 目标线设置

玩家可以设置：

- 直线、C 线、S 线；
- 单独生成一种，或配置三种相对权重；
- 难度；
- 弧长范围；
- 曲率范围；
- 方向范围；
- 玩家作答笔迹显示宽度；
- 目标中心线显示宽度；
- 评分容错半径；
- 是否显示起点和推荐方向；
- Seed。

玩家作答笔宽、目标显示宽度与评分容错半径是三个独立参数。两种显示线宽均不得偷偷扩大评分容错，几何准确度始终使用中心轨迹。

长度优先使用相对于画布短边的比例表示，绝对像素不得作为跨 DPI 的训练参数。

### 7.3 确定性生成

题目标识由以下数据决定：

    GeneratorVersion
    Mode
    BaseSeed
    ExerciseIndex
    NormalizedSettings
    LogicalCanvasWidth
    LogicalCanvasHeight

随机算法必须由项目自行固定版本，不得依赖运行时默认随机实现保证长期复现。

生成流程：

1. 根据权重确定线型；
2. 使用受约束参数构造直线或三次贝塞尔；
3. 使用解析边界检查和自适应采样验证；
4. 检查弧长、边距、端点、曲率、尖点、自交和非相邻自我贴近；
5. 固定次数失败后按确定顺序放宽非核心随机参数；
6. 使用同线型的确定性安全候选；
7. 若设置本身无解，拒绝应用设置并指出具体字段。

禁止无上限随机循环。允许参考线相交只适用于模式二的线条组合，不改变单根线自身不得自交的规则。

### 7.4 评分

评分只使用最终 LogicalStroke 中心轨迹和目标中心线，先进行弧长重采样，再计算：

- Accuracy：玩家轨迹到目标中心线的距离；
- Coverage：目标中心线被玩家轨迹覆盖的比例；
- Smoothness：相对于目标合理曲率后的额外抖动和切线误差；
- Economy：多余长度、折返、来回涂抹和远离目标的额外轨迹。

令 R 为大于 0 的评分容错半径。对玩家等弧长样本到目标折线的点到线段距离，计算 meanDistance 与 p95Distance：

    normalizedError =
        0.70 × meanDistance / R
        + 0.30 × p95Distance / R

    Accuracy =
        100 × exp(-0.85 × normalizedError)

对每个目标等弧长样本计算其到玩家折线的距离 targetDistance：

    Coverage =
        100 × mean(
            exp(-0.50 × (targetDistance / R)²))

Smoothness 使用方向无关的切线差和高频残差。每个玩家样本使用其最近目标线段的局部切线作为比较对象；切线夹角通过绝对点积限制在 0～90°，避免反向绘制被惩罚：

    tangentError =
        mean(acos(abs(dot(userTangent, targetTangent))))
        / (π / 2)

    jitterError =
        meanHighFrequencyResidual / R

    Smoothness =
        100 × exp(
            -1.20 × tangentError
            -1.50 × jitterError)

Economy 使用多余长度和沿目标投影进度的局部折返比例。先选择正向或反向中总体折返更少的方向，再计算 backtrackingRatio：

    excessLength =
        max(0, userLength / targetLength - 1.05)

    Economy =
        100 × exp(
            -1.80 × excessLength
            -3.00 × backtrackingRatio)

空笔迹、单点、近零目标长度和无法计算稳定切线的情况必须进入明确的低分分支，不能产生 NaN 或 Infinity。

默认总分：

    Weighted =
        0.50 × Accuracy
        + 0.30 × Coverage
        + 0.10 × Smoothness
        + 0.10 × Economy

    CoverageCap =
        20 + 80 × (Coverage / 100)^0.65

    Total = min(Weighted, CoverageCap)

所有子分和总分限制在 0～100。

Accuracy 使用点到目标折线线段的距离，而不是按原始采样索引直接比较。Coverage 从目标到玩家的反向距离计算。方向比较必须忽略正反向，不能因为玩家从提示终点开始就直接扣分。

Economy 必须让沿目标来回涂抹、长距离绕路和局部重复覆盖得不到高分。Smoothness 必须比较玩家相对于目标的额外不规则变化，不能把正常 C/S 曲率本身当作抖动。

首版评分参数标记为 LineScoreV1。任何会改变历史分数语义的公式或常数修改必须升级评分版本。

### 7.5 模式一最低验收

- 完美目标轨迹总分不低于 99；
- 正向和反向完美轨迹分差不超过 0.5；
- 同一几何轨迹四倍改变采样密度后分差不超过 2；
- 只描约一半时 Coverage 接近对应水平，且总分受到连续完成度上限限制；
- 单点和极短笔迹返回有限低分，不抛异常；
- 大量沿线来回涂抹不能得到高分；
- 整体偏移增大时 Accuracy 总体单调下降；
- 所有统计值均为有限数。

## 8. 模式二：观察并复制线条组合

### 8.1 界面与提交

界面左右显示两个尺寸完全相同的正方形：

- 左侧为参考图；
- 右侧为玩家绘图区；
- 上方显示实时接近百分比；
- 玩家点击“完成并下一题”手动提交。

玩家可以撤销、重做和清空。提交前的所有操作只改变最终答案，不形成过程评分。

### 8.2 最终答案定义

模式二只比较提交时最终可见的中心轨迹几何集合，不评价：

- 落笔顺序；
- 绘制方向；
- 绘制速度和停顿；
- 一条参考线由一笔还是多笔完成；
- 一笔是否连续经过多条参考线；
- 设备采样率。

笔刷宽度、压力和透明度不参与几何评分。玩家把两条参考线用额外路径连接时，连接路径属于最终答案中的多余几何，应当扣分。

不得强制“一笔对应一条参考线”。原始笔画配对只能用于提交后的诊断，不能成为主评分前提。

参考和玩家答案都转换到正方形 0～1 归一化坐标。评分前不得对玩家答案进行整体平移、旋转或缩放对齐，因为位置、大小和角度就是训练内容。

### 8.3 组合生成

设置包括：

- 固定线条数量，或最少到最多数量；
- 数量范围 1～10；
- 直线、C 线、S 线和相对权重；
- 难度、弧长和曲率；
- 是否允许不同参考线相交；
- Seed。

每根线先在规范化正方形内生成，再进行受约束的尺度、方向和位置变换，最后验证整个组合：

- 所有线完整位于安全区域；
- 禁止单根线自交；
- 禁止近乎完全重合；
- 禁止无法辨认的过度拥挤；
- 禁止过多近平行贴合；
- 不允许相交时检查线段间相交；
- 允许相交时仍限制交点密度和重合长度。

因为模式二不评价绘制过程，所以完全重合线条的“数量”无法从最终几何中辨认。生成器必须禁止完全或近乎完全重合。

### 8.4 实时与最终评分

主评分基于最终中心线几何集合：

- TargetCoverage：参考几何到玩家几何的覆盖；
- UserPrecision：玩家几何到参考几何的精度和多余程度；
- LayoutSimilarity：位置、长度、方向和曲率形成的整体空间布局差异；
- ExtraGeometry：离开参考集合的多余几何。

主评分必须保持分笔、顺序和方向不变性。可以使用双向点到线段距离、距离场或空间索引。参考线与推断几何的最佳匹配只用于结算诊断，不能让原始 pen-down 分段影响总分。

令 R2 为正方形归一化坐标中的容错半径。参考和玩家几何都以固定弧长间隔采样，原始设备点数不作为权重。玩家采样在计权前必须转换为唯一几何并集或等价的占据／距离场；完全重合的重复描画不得增加正确样本权重，也不得稀释额外错误几何的惩罚。

对每个参考样本到玩家几何的距离 dTarget，以及每个玩家样本到参考几何的距离 dUser，计算：

    TargetCoverage =
        100 × mean(
            exp(-0.50 × (dTarget / R2)²))

    UserPrecision =
        100 × mean(
            exp(-0.50 × (dUser / R2)²))

    ShapeF1 =
        2 × TargetCoverage × UserPrecision
        / (TargetCoverage + UserPrecision)

当两项之和为 0 或答案为空时，ShapeF1 为 0。

为了强化整体位置和结构，在固定多分辨率网格上建立参考与玩家的截断中心线距离场。距离统一截断到 4 × R2，只统计至少靠近参考或玩家几何之一的活动网格，避免大量空白背景稀释误差：

    LayoutSimilarity =
        100 × clamp(
            1
            - mean(abs(targetField - userField))
              / (4 × R2),
            0,
            1)

最终分数使用 ShapeF1 作为基础，布局项只能调整已有几何匹配质量，不能让空答案或严重漏画获得高分：

    Total =
        ShapeF1
        × (0.75 + 0.25 × LayoutSimilarity / 100)

ExtraGeometry 作为错误量显示，可由 100 - UserPrecision 及超出容错区域的玩家弧长比例共同报告，不再与 UserPrecision 重复计权。公式与采样间隔标记为 MultiLineScoreV1；修改后必须升级评分版本。

实时评分：

- 目标约 10 Hz；
- 使用当前答案的不可变快照；
- 对旧任务使用取消令牌；
- 每次轨迹变化增加 AnswerVersion；
- 只有题号、Seed、AnswerVersion 和设置版本全部匹配时才能提交 UI 结果；
- 采用 latest-wins，旧结果不得覆盖新结果；
- 不得阻塞输入和渲染线程。

玩家提交时冻结答案并执行一次高精度最终评分。提交后的结果至少显示：

- 总接近百分比；
- 漏画区域；
- 多余区域；
- 位置误差；
- 长度误差；
- 方向误差；
- 曲率误差。

### 8.5 模式二最低验收

- 完美复制不低于 99；
- 改变笔画顺序后分差不超过 0.5；
- 反转每笔绘制方向后分差不超过 0.5；
- 将一条线拆成多笔或将多条线连续画成一笔，只要最终几何相同，分差不超过 1；
- 整体位置错误必须明显扣分；
- 漏掉一根、多画一根和增加连接线必须扣分；
- 1～10 根参考线时实时评分不阻塞绘画；
- 被取消的旧评分任务永远不能更新当前结果。

## 9. 模式三：颜色匹配训练

### 9.1 界面

- 显示足够大的目标色块；
- 使用完整色相环；
- 中央为 HSV 饱和度／明度正方形；
- 正方形左到右为低饱和度到高饱和度；
- 上到下为明亮到黑暗；
- 拖动连续且显示当前选择色；
- 大型试色画布占主要区域；
- 试色画布使用中性灰背景；
- 支持压力、笔刷大小、稳定、撤销、重做和清空。

颜色选择器和目标／选择色块不得使用会改变最终颜色判断的透明度或主题着色。深色与浅色主题进入模式三后都必须切换到经过验证的中性工作背景。

### 9.2 训练方式

- 练习模式：实时显示接近度；
- 测试模式：提交前隐藏接近度；
- 玩家点击提交时，当前选中颜色就是唯一颜色答案；
- 试色画布只用于尝试，不从画布取平均，不参与颜色得分；
- 提交后显示对比和误差，再进入下一题。

### 9.3 目标颜色生成

目标颜色在 OKLab／OKLCh 中按难度桶抽样，再转换到 SDR sRGB。

首版使用拒绝采样保证颜色位于 sRGB gamut 内，不得简单裁剪所有越界颜色到边界，以免目标大量堆积在 gamut 边缘。

难度控制：

- 是否包含接近纯白；
- 是否包含接近纯黑；
- 是否包含低彩度；
- 是否限制在中等明度和彩度；
- 是否缩小目标与上一题的最小感知距离。

颜色生成必须受 GeneratorVersion、Seed、题号和难度设置控制。

### 9.4 颜色评分与报告

目标色和玩家色转换到 OKLab，定义：

    deltaL = 100 × (Lplayer - Ltarget)
    deltaA = 100 × (aplayer - atarget)
    deltaB = 100 × (bplayer - btarget)

    Ctarget = sqrt(atarget² + btarget²)
    Cplayer = sqrt(aplayer² + bplayer²)
    deltaC = 100 × (Cplayer - Ctarget)

    deltaS = 100 × (SplayerHSV - StargetHSV)
    deltaV = 100 × (VplayerHSV - VtargetHSV)

    deltaEOK =
        100 × sqrt(
            (Lplayer - Ltarget)²
            + (aplayer - atarget)²
            + (bplayer - btarget)²)

色相使用 atan2(b, a)，deltaH 取 -180°～180° 的最短环形差。

默认接近百分比：

    Similarity =
        100 × exp(-0.035 × deltaEOK)

相同颜色必须严格返回 100。deltaEOK 增大时 Similarity 必须单调下降。

结算显示：

- 接近百分比；
- deltaEOK 总体感知差异；
- 明度偏亮或偏暗，以及 deltaL；
- 彩度偏鲜艳或偏灰，以及 deltaC；
- HSV 饱和度偏高或偏低，以及 deltaS 百分点；
- HSV 明度偏高或偏低，以及 deltaV 百分点；
- 色相偏移方向和最短角度 deltaH；
- 可选显示偏红／偏绿的 deltaA；
- 可选显示偏黄／偏蓝的 deltaB；
- 目标色和玩家色并排对比。

当目标色或玩家色满足以下任一条件时，不报告误导性色相方向：

- OKLCh 彩度低于 0.02；
- OKLab 明度低于 0.05；
- 数值误差使色相不稳定。

此时显示“色相差异不可判定”。

颜色公式标记为 ColorScoreV1，修改色彩空间、gamut 策略或百分比映射时必须升级版本。

### 9.5 模式三最低验收

- 相同颜色严格为 100；
- deltaEOK 增大时接近百分比单调不增；
- 色相差使用正确的 0°／360° 环绕；
- 灰色和近黑颜色不会输出虚假的色相方向；
- 改变试色画布内容不改变同一当前选中色的得分；
- 固定色相和 HSV 明度时，饱和度变化的方向与 deltaS 符号一致；
- 改变压力、笔宽或稳定值不改变颜色得分；
- 目标颜色始终是有限且可显示的 sRGB 颜色；
- 固定 Seed 和设置产生相同颜色序列。

## 10. 页面、交互和快捷键

至少包含：

- 首页与三种模式入口；
- 每种模式的设置页或设置面板；
- 训练中的实时数据；
- 单题结算；
- 本次会话统计；
- 历史最佳；
- 深色和浅色主题；
- 首次进入的简短引导。

风格应克制、现代、专业，禁止儿童化装饰和干扰绘画的动画。

快捷键默认建议：

| 操作 | 模式一 | 模式二 | 模式三 |
|---|---|---|---|
| Space | 暂停／继续 | 暂停／继续 | 暂停／继续 |
| N | 跳过当前题 | 跳过当前题 | 跳过当前题 |
| Ctrl+Z | 不适用 | 撤销 | 撤销试色笔迹 |
| Ctrl+Y | 不适用 | 重做 | 重做试色笔迹 |
| Delete | 不适用 | 清空答案 | 清空试色画布 |
| Enter | 不适用 | 手动提交 | 提交当前颜色 |
| Esc | 暂停或关闭设置 | 暂停或关闭设置 | 暂停或关闭设置 |

文本输入框获得焦点时不得拦截正常编辑快捷键。正在落笔时，可能破坏当前答案的命令必须禁用或先执行明确的 SystemCancelled，不得悄悄丢失答案。

## 11. 设置、统计和本地数据

设置与历史保存在本机 LocalAppData 下的 DrawAim 目录。

设置至少保存：

- SchemaVersion；
- 每种模式的难度和生成设置；
- Seed 和 GeneratorVersion；
- 线型权重；
- 显示设置；
- 稳定值和 StrokeStabilizerVersion；
- 主题；
- 笔刷和压力显示设置。

历史每题至少保存：

- 时间；
- 模式；
- Seed、题号和生成器版本；
- 难度与设置指纹；
- 稳定值与稳定器版本；
- ScoringVersion；
- 总分和子分；
- 是否跳过或系统取消。

最佳成绩必须按可比设置分组。至少区分：

- 模式；
- 标准难度或自定义；
- 评分版本；
- 无辅助或稳定辅助；
- 会影响分数的设置指纹。

不能把最大容错、稳定 100 的辅助成绩与默认容错、稳定 0 的成绩混成同一最佳成绩。模式三的试色稳定值不影响颜色评分，因此不进入颜色成绩分组键。

文件要求：

- 首次运行使用安全默认值；
- 损坏 JSON 不得导致启动崩溃；
- 损坏文件备份后恢复默认；
- 临时文件写入后原子替换；
- 未知字段忽略，缺失字段补默认；
- 日志滚动并限制数量和总容量；
- 不保存完整长期笔迹，除非用户主动导出；
- 冒烟测试支持将数据根目录重定向到临时测试目录，避免污染真实用户数据。

## 12. 性能、并发与内存预算

所有性能结果必须记录测试机器、屏幕刷新率、输入设备和构建配置。

首版目标：

- 1920×1080 和 2560×1440 下绘画目标为稳定 60 FPS；
- UI 帧耗时 P95 不高于 16.7 ms；
- 60 秒持续绘画测试中不得出现超过 50 ms 的周期性卡顿；
- 输入事件到对应笔迹进入渲染树的处理耗时 P95 目标小于 8 ms（不含显示器扫描延迟）；
- 输入点清理与稳定每批 64 点 P95 小于 0.5 ms；
- 模式一最终评分 P95 小于 50 ms；
- 模式二 10 根参考线时预览评分约 10 Hz；
- 模式二预览任务 P95 小于 80 ms；
- 评分、生成和保存不得阻塞输入线程；
- 后台任务使用不可变快照、取消令牌和版本校验；
- 连续完成 1000 题后，暖机后的托管内存不得持续线性增长；
- 撤销栈、笔画数、点数、历史详情和日志均有明确上限。

高稳定值产生的几何跟随感是用户主动选择的算法效果，不能用线程排队或固定等待伪造。输入事件必须及时处理，不得积压旧包。

## 13. 错误处理

以下情况不得使应用崩溃：

- 设置或历史 JSON 损坏；
- 本地目录暂时不可写；
- 画布宽高为 0 或过小；
- 权重全部为 0；
- 生成设置无解；
- 生成器达到尝试上限；
- 空、单点或非法笔迹；
- 快速重复落笔；
- 评分任务过期；
- 窗口快速缩放和 DPI 切换；
- Capture 丢失；
- 压力、坐标或时间戳异常；
- 应用在绘画中关闭。

非致命错误使用非阻塞提示并恢复到安全状态。致命错误显示简洁中文说明和本地日志路径。日志不得上传网络，也不得记录不必要的完整玩家笔迹。

## 14. 自动化测试与验收矩阵

### 14.1 自动测试

至少覆盖：

- 固定随机算法和 Seed 派生黄金向量；
- 相同 GenerationKey 产生相同题目；
- 不同题号通常产生不同题目；
- 单线和组合不越界；
- 大样本下线型、方向和位置分桶命中率符合设置权重且无系统性空区；
- 单线不自交、不出现 cusp 和零长度；
- 模式二禁止重合和拥挤的规则；
- 弧长、重采样、点到线段距离和相交；
- 稳定器 0 的身份、强度单调性、采样率一致性和 maxLag；
- 模式一完美、半程、偏移、涂抹、单点和正反方向；
- 模式二顺序、方向、拆笔、合笔不变性；
- 模式二重复描相同几何不加权，且不能稀释额外几何惩罚；
- 模式二漏画、多画、错位和旧任务取消；
- 模式三 sRGB／OKLab 转换、色相环绕、灰色降级和单调色差；
- 模式三 HSV 饱和度和明度差的方向与数值；
- 设置损坏恢复、保存读取和 Schema 迁移；
- 状态机、系统取消、重复设备事件和输入锁。

测试断言必须使用明确数值，禁止只写“明显”“基本一致”或为了通过而降低标准。

### 14.2 当前机器 GUI 冒烟

实际启动 Release 或发布产物，完成：

1. 模式一至少连续作答 20 题；
2. 验证任意画布位置落笔、正反方向、短笔和自动下一题；
3. 模式二完成 1 根、5 根和 10 根线的绘制、撤销、清空和提交；
4. 模式三完成练习、测试、试色、换色和提交；
5. 切换深浅主题；
6. 在模拟 1920×1080 与 2560×1440 工作区尺寸下检查布局；
7. 检查日志无未处理异常；
8. 使用重定向测试数据目录，不污染真实用户设置。

### 14.3 实体硬件验收

以下项目只有实际具备条件并完成测试后才能标记通过：

- 实体压感数位笔；
- 鼠标与数位笔模拟事件去重；
- 0、25、50、75、100 的稳定手感；
- 125%、150%、200% DPI；
- 多显示器跨屏 DPI；
- 实体 1080P 与 2K 显示器；
- Windows 10 与 Windows 11；
- 高频快速绘画的主观拖手和延迟。

没有设备时应明确标记“未验证”，不能用代码检查或单元测试冒充实体硬件验证。

## 15. 分阶段实施 Gate

阶段划分只规定实施和验收顺序，不删减最终范围。

### Gate 1：共享输入与模式一垂直切片

- 技术选型和工程骨架；
- 鼠标、数位笔、压力、DPI 和输入捕获；
- StrokeStabilizerV1；
- 单线生成、单笔自动提交和 LineScoreV1；
- 最小训练 UI；
- 自动测试、Release 构建和启动冒烟。

Gate 1 未达到输入和帧率预算前，不进入模式二。

### Gate 2：模式二

- 规范化双画布；
- 线条组合生成；
- 最终几何集合；
- 撤销、重做和清空；
- 10 Hz 异步预览；
- 手动提交和误差分析；
- 顺序、方向、拆笔和合笔不变性测试。

### Gate 3：模式三

- 色相环和 HSV 方块；
- OKLab／OKLCh 目标生成；
- 大型试色画布；
- 练习与测试模式；
- ColorScoreV1 和维度报告；
- 色彩黄金测试。

### Gate 4：整体完善与发布

- 首页、完整设置、主题和引导；
- 会话统计、历史和最佳成绩分组；
- 损坏恢复、日志和异常处理；
- 全量性能与回归；
- README 和构建报告；
- win-x64 自包含发布；
- 当前机器 GUI 冒烟；
- 记录未完成的实体硬件验证项。

## 16. 构建、发布和交付

最终必须实际执行：

- 依赖还原；
- 静态分析或编译器检查；
- 全部自动化测试；
- Release 构建；
- win-x64 发布；
- 发布产物启动冒烟。

发布目录固定在项目 artifacts 下。构建报告至少记录：

- 实际 SDK 与目标框架；
- 操作系统；
- 测试数量与结果；
- Release 构建结果；
- 发布架构和发布形式；
- EXE 或启动文件的绝对路径与相对路径；
- 文件大小和 SHA-256；
- GUI 冒烟结果；
- 实体硬件和系统兼容性中尚未验证的项目；
- 已知限制。

README 至少包含：

- 产品用途和三种模式；
- 三种答案与提交规则；
- 笔迹稳定 0～100 的含义；
- 评分原理和评分版本；
- 快捷键；
- 设置和历史位置；
- 构建、测试和发布命令；
- 运行要求；
- 已知限制。

## 17. 完成定义

只有同时满足以下条件，首个正式版本才算完成：

1. 三种模式都能从进入、训练、提交、结算到下一题形成完整闭环；
2. 所有可见按钮和菜单都有真实功能；
3. 模式一严格执行单笔抬起自动提交；
4. 模式二只评价最终几何答案并手动提交；
5. 模式三只评价当前选择颜色并报告感知维度；
6. 笔迹稳定从 0 到 100 连续可调，0 真正关闭，100 效果强烈但不读取目标；
7. 显示轨迹与评分轨迹一致；
8. 关键生成、评分、稳定和颜色算法具有自动化测试；
9. 所有测试、Release 构建和发布成功；
10. 发布产物实际启动并完成当前环境可执行的三模式冒烟；
11. 性能结果达到预算或在报告中提供可复现证据和修复结论；
12. 没有假按钮、随机评分、静态画布、核心 TODO 或伪造验证；
13. 无法执行的数位笔、DPI、双系统和实体显示器验证被如实列为未验证。

## 18. 已固定的产品决策摘要

- 模式一：任意画布内落笔，一笔作答，抬笔自动提交；
- 模式一起点和方向只是提示，正反向评分应近似一致；
- 模式一系统中断不计分；
- 模式一分项为准确度、覆盖率、平滑度和多余轨迹；
- 模式二：玩家手动提交，只看最终中心轨迹几何；
- 模式二不看顺序、方向、速度、停顿和分笔方式；
- 模式二不允许整体对齐后评分；
- 模式三：提交当前选中色，试色画布不参与颜色评分；
- 模式三使用 OKLab／OKLCh 计算总体色差，并同时报告明度、感知彩度、HSV 饱和度、HSV 明度和色相；
- 笔迹稳定：0～100，默认 0，100 强烈但不吸附目标；
- 稳定后的显示中心线就是评分中心线；
- 稳定辅助成绩与无辅助成绩分开；
- Seed 复现包含算法版本、模式、题号、设置和画布配置；
- 默认未锁定 Seed，每道新题换新；只有玩家明确锁定 Seed 时才复现确定性序列；
- 无解设置必须拒绝或明确修正，不能无限随机重试；
- 自动验证、当前机器冒烟和实体硬件验收分别报告。

## 19. 当前实现记录

2026-08-03 已完成 DrawAim `0.1.1`：三种训练模式、共享鼠标／Stylus 输入、0～100 稳定器、版本化确定性生成与评分、本地设置／历史、深浅主题、首次引导、紧凑布局和 win-x64 自包含发布均已落地。

`0.1.1` 根据实际使用反馈完成以下修正：

- 模式一拆分“玩家作答笔宽”和“目标参考线宽”，玩家笔宽可在本题实时调整，评分仍只使用中心轨迹；
- 未锁定时每道新题使用与上一题不同的新 Seed，锁定后才按 Seed、题号和设置复现序列；
- 单线生成器升级到 `LineGeneratorV2`，消除受限设置连续进入固定 fallback 时的结构性重复，并对方向、位置、长度、曲率和曲率符号进行分层；
- 模式一、模式二浅色主题使用白画布／黑笔，深色主题使用深画布／浅笔，已有答案随主题安全重绘；模式三保留不受主题干扰的中性灰试色画布；
- 本地设置记录各模式真实生成器版本，不再残留通用占位版本。

最终 Release 构建为 `0 warning / 0 error`，自动化测试为 `78 / 78`，发布 EXE 已完成两次启动的三模式 GUI 冒烟、主题像素断言、逐题 Seed 变化断言和设置恢复断言。精确环境、命令、发布物哈希、截图证据及尚未实机验证的硬件／DPI 项目见 [BUILD_REPORT.md](./BUILD_REPORT.md)。本节只记录实现状态，不放宽前述产品规则和后续实体硬件验收要求。
